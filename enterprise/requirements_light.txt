# PLM Enterprise - Optimized for Render Free Tier (512MB memory)
# Uses HuggingFace Inference API for embeddings instead of local model
# This avoids loading PyTorch (~500MB) and sentence-transformers

# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-dotenv==1.0.0
pydantic==2.5.3

# Database
supabase==2.3.0
psycopg2-binary==2.9.9

# Vector/Math operations (lightweight)
numpy==1.24.3

# API Clients
openai==1.10.0
groq==0.4.1
httpx==0.26.0

# Utilities
requests==2.31.0
aiohttp==3.9.1
python-multipart==0.0.6

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
